@if (Request.IsAuthenticated)
{
    <nav-account>
        <ul>
            <li>
                <a href="#"><span class="glyphicon glyphicon-user"></span> jozko.mrkvicka@email.com<div id="down-triangle"></div></a>
                <ul>
                    <li><a href="#">Moje inzeráty<div class="circle"></div></a></li>
                    <li><a href="#">Moje dopyty<div class="circle"></div></a></li>
                    <li><a href="#">Nastavenie<div class="circle"></div></a></li>
                    <li><a href="#">Zmena hesla<div class="circle"></div></a></li>
                    <li><a href="#">Odhlásiť sa<div class="circle"></div></a></li>
                </ul>
            </li>
        </ul>
    </nav-account>
}
else
{
    <input type="button" name="btn" class="transparent-btn" onclick="location.href='@Url.Action("RegisterUser", "Account")'" value="REGISTRÁCIA" />
    <input type="button" name="btn" class="transparent-btn" data-toggle="modal" data-target="#e-login-form" value="PRIHLÁSENIE" />

     <!-- Modal -->
    @*<div class="modal fade popup-form" id="e-login-form" role="dialog">
            <div class="container">
                <div class="row">
                    <div class="col-md-3 col-md-offset-8">
                        <div class="account-box">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close" aria-hidden="true">&times;</button>
                            <div class="logo ">
                                <h3>eBado</h3>
                            </div>
                            @if (ViewBag.ErrorMessage != null)
                            {
                            <div class="alert alert-danger" role="alert">
                                <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
                                @ViewBag.ErrorMessage
                            </div>
                            }
                            <form class="form-signin" action="#">
                                <div class="form-group">
                                    @Html.EditorFor(model => model.Email, new { htmlAttributes = new { placeholder = "E-mail", @class = "form-control" }, required = "required" })
                                </div>
                                <div class="form-group">
                                    @Html.EditorFor(model => model.Password, new { htmlAttributes = new { placeholder = "Heslo", @class = "form-control" }, required = "required" })
                                </div>
                                <label class="checkbox">
                                    <input type="checkbox" value="remember-me" />
                                    Zapamataj si ma
                                </label>
                                <button class="btn btn-lg btn-block login-bg" type="submit">
                                    Prihlasit
                                </button>
                            </form>
                            <a class="forgotLnk" href="#">Zabudol som heslo</a>
                            <div class="or-box">
                                <span class="or">ALEBO</span>
                                <div class="row">
                                    <div class="col-md-12 row-block">
                                        <a href="#" class="btn btn-primary btn-block">Vytvorit novy ucet</a>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>*@


}
